"use client"
import {MemberProps} from "@/features/members/type";

export default function ModifyMemberForm({members} : {members : MemberProps[]}) {


    return (<>

        <section>
            <form>

            </form>
        </section>

    </>)
}